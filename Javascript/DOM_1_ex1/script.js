//DOM=>Document Object Model => link the JS object to the HTML document

//Selecting methods => Getting HTML elements by their class name

//const heading = document.getElementsByClassName("title");
//console.log(heading);

//Selecting methods => Getting HTML Elements by their ID

//let dogImage = document.getElementById("dog-img");

//console.log(dogImage);

//Selecting methods => Selects all elements that share that selector

//let items = document.querySelectorAll(".item");

//console.log(items);

//Selecting methods => Selects only one element that has the selector

let dogImage = document.querySelector("#dog-img");

console.log(dogImage);

